# スーパーLive2D「ベクターパペット」方式 — スマホ+Claudeのみで完結する手法(2026-07-19)

> `VTUBER_ASSET_ENGINE_PLAN.md`の制約(サンドボックスにGPU無し、外部AI禁止)を
> 受けて考案した、**画像生成AIを一切使わない**「スーパーLive2D」の実現手法。
> 既存計画の置き換えではなく、計画のボトルネック(アセット生成)を別原理で
> 解く代替生成器として位置づける。**PoC実装・実機検証済み**
> ([`poc/vector_puppet_poc.html`](./poc/vector_puppet_poc.html))。

## 1. 制約の整理(この手法の出発点)

| 制約 | 帰結 |
|---|---|
| 外部AI禁止(Veo・Gemini API・クラウド画像生成すべて不可) | 実行時LLMルーティングも生成もクラウドに頼れない |
| ユーザーはスマホのみ所持 | 成果物はモバイルブラウザで動く単一HTMLが理想 |
| サンドボックスはCPUのみ(GPU無し、実機確認済み) | SD系ローカル画像生成は1枚数分〜数十分で、数百枚規模は非現実的 |
| Claude・サンドボックス内ローカルAIは使用可 | **Claude自身を「生成器」にできる** |

既存計画の本命案(パーツ×角度×表情をローカル画像生成AIで量産)は、この
制約下では生成パイプラインが成立しない。そこで発想を反転させる。

## 2. 核となるアイデア: キャラクターを「画像」ではなく「コード」として生成する

**Claudeは画像をピクセルとして描くのは苦手だが、パラメトリックな図形を
コードとして記述するのは得意**であり、しかもテキストフィードバックだけで
無限に修正できる。そこで:

- キャラクター = **パラメトリックな2.5Dパーツ定義の集合(JavaScriptコード)**
- レンダリング = **モバイルブラウザのCanvas 2Dによるトゥーン描画**(実行時)
- 生成AI = **不要**。素材画像ゼロ。アセットは全てコードから導出される

これを「ベクターパペット」方式と呼ぶ。Live2D が「少数の絵をワープして
回転を偽装する」のに対し、この方式は**全パーツが軽量な実3D情報を持ち、
任意角度を順方向レンダリングする**。見た目はアニメ調2Dのまま。

### なぜこれが「スーパー」Live2Dか

| | Live2D | 既存計画(AI画像差分) | ベクターパペット |
|---|---|---|---|
| 大角度回転 | ワープ破綻 | ○(実角度の絵) | ○(実3Dから描画) |
| ツインテール細り | — | ○ | ○(順方向描画なのでvisual hull限界が原理的に無い) |
| 角度の連続性 | ○(連続変形) | △(N方向+クロスフェード) | **○(完全連続、1°刻みも可)** |
| Z順の入れ替わり | 手動設定 | 角度バケットごとのメタデータが必要 | **毎フレーム深度ソートで自動解決** |
| アセット生成コスト | 絵師の手作業 | GPU必須(この環境では不可) | **ゼロ(コードのみ)** |
| 修正のしやすさ | 専用ツール | 再生成ガチャ | **テキストで指示→Claudeがコード修正** |
| 画風の上限 | 原画そのまま | 生成モデル次第 | フラット/トゥーン調に限定(後述) |

## 3. アーキテクチャ

### 3.1 パーツの3類型(プリミティブ)

全パーツは以下3種のプリミティブのどれかとして定義する:

1. **card(球面カード)** — 目・眉・口・頬紅など顔の要素。頭部球面上の
   (方位角, 仰角)に貼り付き、頭の回転に応じて球面上を滑走する。
   視線との角度に応じて横方向に`cos`縮小し、輪郭ぎわで
   フェードアウト+カリング(PoC実測: しきい値 `facing > 0.32` が良好)。
2. **ribbon(リボン/チューブ)** — ツインテール・アホ毛・腕など細長い
   もの。3D空間のスパイン(制御点列)+幅プロファイルで定義し、
   **投影後に画面空間で幅をオフセットして輪郭を作る**。チューブ半径が
   画面上そのまま幅になるため、視線方向に伸びる区間でも幅が潰れない。
   これが**ツインテール・エッジオン問題の構造的解**(visual hullは復元で
   min交差が効くから細るが、こちらは復元をしないので細る機構が存在しない)。
3. **volume(ボリューム)** — 頭(球)・胴・スカートなど塊。輪郭曲線+
   見かけ幅関数(正面/背面=1、真横=0.6程度)で描く。トゥーン影は
   光源方向の三日月クリップで付ける。

### 3.2 Z順(計画書§2の課題の解)

各パーツが `depth()`(ビュー空間zの代表値)を返し、**毎フレーム
深度ソートしてから描く**だけで、「正面ではツインテールが体の前、背面では
後ろ」が自動的に正しくなる。角度バケットごとのZ順メタデータは不要になる。
PoCで全周にわたり破綻しないことを確認済み。

### 3.3 表情(VRMプリセット互換)

計画書§3の方針どおりVRM表情タクソノミーを採用。表情=顔カードの
キー形状パラメータセット(`eyeOpen / eyeCurve / browY / browTilt /
mouth / blush`)で、`happy / angry / sad / relaxed / surprised`+
まばたき+viseme(aa/ih/ou/ee/oh は口カードのキー形状追加のみ)を表現
する。ベクターのキー形状は制御点の線形補間でモーフでき、中間表情も連続。

### 3.4 モーション

- 一次: `yaw / pitch / roll`(頭・体で追従率を変える。PoCでは体=0.55倍)
- 二次: 髪・リボンはスパイン制御点への正弦/ばね変位(PoC実装済み、
  将来は Verlet ばねに置換可)
- 全てCPUの軽い算術のみ。スマホブラウザで60fps(Canvas 2D)

### 3.5 Claude視覚フィードバックループ(この手法の「生成AI」に相当する部分)

サンドボックスにはChromium+Playwrightが同梱されている。これで
**Claudeが自分の描いたキャラを自分の目で見て直す**閉ループが組める:

```
Claudeがパーツコードを記述
  → ヘッドレスChromiumで複数yaw×表情をスクリーンショット(数秒/枚)
  → Claudeが画像を読んで問題を発見
  → コード修正 → 再撮影 → …
```

PoCではこのループを2周して5件の視覚的問題(前髪が目に被る、耳の
プリミティブ破綻、背面の頭皮露出、背面の体幅・腕の破綻、輪郭ぎわの目の
リング化)を発見・修正した。**拡散モデルの「ガチャ」と違い、修正が決定的
(deterministic)で、直した箇所以外は絶対に変わらない**のがコード生成の
強み。ユーザー側はスマホでHTMLを開き、「前髪をもう少し重く」のような
自然言語フィードバックを返すだけでよい。

## 4. PoC実証結果(2026-07-19、本セッション)

[`poc/vector_puppet_poc.html`](./poc/vector_puppet_poc.html) —
**単一HTML約550行、依存ライブラリゼロ、画像素材ゼロ**。スマホブラウザで
そのまま動く(yaw/pitchスライダー、表情5種、まばたき、自動回転)。

検証済みスクリーンショット([`poc/screenshots/`](./poc/screenshots/)):

| 検証項目 | 結果 |
|---|---|
| yaw 0°/45°/90°/135°/180° | 全角度で破綻なし。**ツインテールは全周で太さを維持**(エッジオン細りなし) |
| Z順の自動入れ替わり | 45°で奥のテールが頭の後ろへ、135°で手前のテールが後ろ髪の前へ、自動で正しく遷移 |
| 表情 happy / surprised | にっこり目+頬紅、見開き+丸口が明瞭に読める。角度と独立に合成される |
| 顔カードの球面滑走 | 45°で目・口が顔の回転に追従、90°付近でカリング(輪郭ぎわアーティファクトは修正済み) |
| レンダリングコスト | ヘッドレス撮影で1枚1秒未満(CPUのみ)。SD系の数分〜数十分/枚と桁違い |

## 5. 既存アセットエンジン計画との接続

この方式は計画書のランタイム設計(静的アセット+LLMルーティング)と
矛盾しない。接続点は2つ:

1. **リアルタイムモード**: ベクターパペットをそのままクライアント描画
   エンジンにする。画像IDの代わりに `{yaw, pitch, expr, …}` パラメータを
   状態遷移の出力とする。角度は完全連続なので32方向制限も消える。
2. **スプライト書き出しモード**: ヘッドレスChromiumで
   「N方向×表情」を透過WebPに一括書き出し(1枚1秒未満 → 数百枚でも
   数分)、計画書どおりの静的アセット群を供給する。**つまり本方式は
   計画の『ローカル画像生成AI』の座にそのまま収まる代替生成器**であり、
   ランタイム側の設計変更は不要。

パーツタクソノミー(計画書§3)もそのまま適用する: コア(頭・顔カード・
前髪・後ろ髪・体・腕)+オプション(ツインテール・リボン・アホ毛等)。
`ghostitd`の`accessories.js`の「体は共通、アクセサリー可変」構成を
プリミティブ種別(card/ribbon/volume)+取り付け座標のJSONマニフェスト
として再定義すれば、キャラごとの差分はマニフェスト+パレットだけになる。

## 6. 限界(正直な記述)

- **画風の上限**: フラット塗り+トゥーン影+ハイライトまで。厚塗り
  イラスト級の質感は出ない。「可愛い」はデフォルメ(ちび〜アニメ調)の
  方向で狙う。ラスター質感が最終的に必要になった場合のみ、将来
  GPU環境を得てからLoRA等を検討すればよく、その際も本方式のスプライト
  出力がControlNet用の完璧な角度ガイド(構図・Z順・パーツ分割済み)に
  なるため無駄にならない。
- **複雑なキャラはコード量が増える**: 装飾が多いほどパーツ定義が伸びる。
  ただし増えるのは宣言的なパラメータであり、Claudeが管理する分には
  実務上の問題は小さい。
- **品質はイテレーション依存**: 一発生成では平凡。視覚フィードバック
  ループとユーザーのスマホからの言語フィードバックで詰める前提の手法。

## 7. ロードマップ(提案)

1. ✅ PoC: 単一HTMLで全周回転+表情+揺れ(本コミット)
2. パーツ定義とレンダラの分離: キャラをJSONマニフェスト
   (プリミティブ種別+取り付け+パレット)に外出しし、レンダラを共通化
   (計画書「次に着手しうる作業a」のマニフェスト設計と統合)
3. viseme口形状5種+ばね物理(Verlet)追加
4. スプライト書き出しCLI(Playwright)→ 既存計画のランタイムに接続
5. キャラデザイン反復: ユーザーがスマホで見て言語フィードバック→
   Claudeが視覚ループで修正、のサイクル運用
