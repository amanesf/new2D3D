# 実イラストをそのまま動かす — マスタープラン(2026-07-19 全面改訂v2)

> 本書が現行の統合計画。要件は3つ: **VTuberをやりたい/可愛くしたい/
> 3Dゲームにも使いたい**。実装はユーザーの明示指示があるまで行わない。
>
> 経緯の詳細は [`AI_NATIVE_SUPER_LIVE2D_REVIEW.md`](./AI_NATIVE_SUPER_LIVE2D_REVIEW.md)
> (スーパーLive2Dの成立条件)と
> [`ON_DEVICE_GENERATION_PLAN.md`](./ON_DEVICE_GENERATION_PLAN.md)
> (オンデバイス生成の調査、ビュー合成部分は本書に置き換え)を参照。

## 0. 設計原則(全決定の上位に置く)

1. **可愛さは常に2D。3Dは裏方。** アニメ顔の可愛さは角度ごとの
   「絵の嘘」でできており、正直な3D形状では原理的に再現できない。
   よって画面に出るピクセルは常に2D由来(原画+2D生成ビュー+局所差分)
   とし、3Dメッシュは幾何の裏付け(整合保証・角度ガイド・Z順解決)
   専用で画面に出さない。3Dゲームでもキャラは**ビルボードスプライトが
   正式な主役**。
2. **実絵キーフレームが主役、ワープは補間糊。** キーフレームを生成で
   増やせることがAIネイティブの本質(ビューリング8方向以上が目標)。
3. **全体再生成をしない。** 同一性はガチャに晒さない。生成は
   「新規ビュー合成(構図ごと)」か「局所差分(目・口だけ)」の
   どちらかに限定し、顔まわりは最終的に原画ピクセルから再合成して固定。
4. **疎格子。** フル分解(全パーツ+表情+口パク)は正面±45°の
   3ビューのみ。90°以遠は簡易分解(一枚絵+髪揺れレイヤー)。
   表情差分は正面近傍にしか載せない。

## 1. アセット供給の序列(モデル選定済み)

| 役割 | 担当 | 根拠 |
|---|---|---|
| ビューリング生成(主役) | **CharacterGen**(デモ利用) | アニメキャラ専用学習・一貫マルチビュー出力・**Aポーズ自動正規化**(Tポーズ問題を同時解決) |
| 幾何の裏方(控え) | Hunyuan3D系 | メッシュ品質は高いが画風はアニメ特化でない。Z順・角度ガイドが2D手法で足りない時のみ |
| ビュー合成の保険 | Zero123 | 本キャラで画風保持を実証済みだが低解像度・世代遅れ |
| 局所差分(表情・口) | Gemini局所編集 | 「目だけ閉じて」等の同一性保持が確実な使い方に限定 |
| 仕上げ・清書 | Pixel 10 Pro(Local Dream等)+Claude | アップスケール→低ノイズimg2img→原画からの顔再合成 |
| 錨 | 既存4面(front/back/side_left/side_right) | 検収済みの実絵。照合リファレンス |

## 2. ワークフロー(5段)

役割分担: **生成=ユーザー(ブラウザ/スマホ)、加工・組立・QC=Claude**。

1. **キーフレーム生成**(ユーザー): 正面原画をCharacterGenデモへ入力
   → Aポーズ正規化済みマルチビューを `characters/gen/` にアップ。
2. **検収と仕上げ**(Claude): 目視QC(①画風保持 ②Aポーズ化の破綻
   ③ビュー間整合)→ アップスケール → 低ノイズimg2img清書(必要時
   Pixelに依頼)→ **目・口・顔まわりだけ原画から再合成**。不合格は
   再生成依頼か隣接ビューからのモーフィング補完。
3. **レイヤー分解とリグ**(Claude): 疎格子原則で分解。切り出し領域・
   アンカー・Z順・スパインを**JSONマニフェスト**に宣言(修正・次キャラ
   再適用はマニフェスト編集のみ)。透過抜きは白閾値でなく**境界からの
   flood fill**(目のハイライト・柄の白線に穴を開けない)。
4. **表情差分**(ユーザー+Claude): Gemini局所編集で閉眼/笑み目/驚き/
   怒り眉/口5形状 → Claudeが目・口領域だけ切り出して登録。
5. **書き出し**(Claude): 同一資産から2系統
   - **VTuber**: 単一HTMLピクセルパペット+MediaPipe顔トラッキング
     (スマホ内ローカル)。±30°ワープ+キーフレーム間クロスフェード。
     OBS取り込みで配信。
   - **3Dゲーム**: ヘッドレスChromiumでN方向×表情スプライトを一括
     書き出し → three.js/Godotのビルボード。

## 3. 品質リスクと対策(レビューで特定済み)

- **Tポーズ問題**: 錨4面もZero123出力もTポーズで、腕を下ろす運用ポーズと
  乖離 → CharacterGenのAポーズ正規化で主解決。落ちた場合はGemini局所
  編集で腕下ろし差分+VTuber用は上半身クロップで回避。
- **解像度の段差**: 生成ビューは錨より粗く、リングを回すと錨で急に
  シャープになる → 仕上げチェーン(§2段2)を全生成ビューに常設。
- **Z順メタデータの復活**: ベクターパペットが自動深度ソートで消した
  ビュー別Z順問題がピクセル方式では戻ってくる → ビューごとの
  Z順をマニフェストに宣言。疎格子でビュー数自体を絞って作業量を抑制。
- **錨4面の相互矛盾**: back.pngにのみ花飾りがある等、錨自身が未検収 →
  生成ビューと突き合わせる前に錨の検収基準(採用ディテールの確定)を
  先に決める。
- **CharacterGen不発の場合**: Zero123+仕上げチェーンに後退(実証済みの
  地点なので失うものは無い)。

## 4. 着手順(生成待ちでMVPを止めない並び)

1. **正面のみVTuberパペット(MVP)**: 既存front.pngだけでレイヤー分解→
   まばたき・口パク・±30°ワープ→MediaPipe接続。生成AI不要で
   3要件のうちVTuberと可愛さを先に回収。
2. 並行: CharacterGenデモ検証(ユーザー)/錨4面の検収基準決め/
   Gemini表情差分の試作。
3. ビューリング整備(CharacterGen→仕上げ→疎格子分解)。
4. スプライト書き出し→three.js最小シーンでビルボード実証(3Dゲーム要件)。
5. 将来オプション: キャラ専用LoRA(同一性の追加補強)、Hunyuan3D裏方
   導入、Live2D的な物理強化(Verletばね)。

## 5. ユーザーの手数まとめ

①CharacterGenデモに1枚入れる ②(必要時)Pixelでimg2img清書を数枚
③Geminiで表情差分を数枚 ④スマホで動作確認して言語フィードバック。
それ以外の工程(QC・分解・リグ・エンジン・書き出し)は全てClaude側。

## 6. 過去の試作との関係

- `poc/vector_puppet_poc.html` — エンジン資産(Z順ソート・スパインばね・
  視覚QCループ)を流用。キャラ描画方式としては不採用。
- `characters/blonde_twintails.html` — ベクター再描画の不採用実例
  (画風乖離の記録として保存)。
- 旧`REAL_ART_LIVE2D_PLAN.md`(v1)の内容は本書に統合。ビューモーフィング
  単独案・img2img主経路案はそれぞれ「補完手段」「仕上げチェーン」として
  §1〜3に吸収した。
