# VTuberアセットエンジン計画書(アイデア段階、2026-07-19)

> **移設メモ(2026-07-19)**: 本計画書は`amanesf/ghostitd`リポジトリで
> 起票されたが、以後の検討・実装はこの`new2D3D`リポジトリで継続する。
> `ghostitd`側の`3Dtool`(`landmark_tool.html`/`character_3d.html`/
> `controller.html`/`js/`パイプライン)は動画→3D化(visual hull)の
> 実装が置かれている参照元であり、本文中の相互参照(例: §3の
> `js/accessories.js`のアクセサリー体系)は`ghostitd`側のパスを指す。

## 位置づけ・経緯

`VEO_VIDEO_TO_3D_PLAN.md`のStage B(特徴追跡による同時較正)・Stage C
(アンカー↔hull SDF融合)を実装・実測した結果、**シルエット交差(visual hull)
という手法そのものの数学的な限界**にぶつかった(詳細は同ファイル§16参照)。
ツインテールのような、体の回転軸から外れた方向へ張り出す細長い/偏平な
パーツは、回転一周のどこかで必ずエッジオン(ほぼ真正面から見た状態)になる
角度が存在し、その角度のシルエットが「一番厳しい制約」として交差(min操作)
に効いてしまうため、**较正やアンカー補正をいくら精密化しても原理的に細り
続ける**。この限界の発見を受けて、ユーザーから「そもそも動画からの3D化を
続けるべきか」という問いが出て、代替アーキテクチャの検討に移った
(2026-07-19、本セッション後半)。

**現時点でのステータス: アイデア段階。まだ何も実装していない。**
`VEO_VIDEO_TO_3D_PLAN.md`のStage B/Cの実装(`js/feature_tracks.js`、
`js/multiview_hull.js`の`fuseAnchorsIntoHull`)はコード自体は`main`に
コミット済みだが、Stage Cの「hull=シルエット整合が上限」という設計の
再考(ゼロベース見直し、ユーザー指摘により中断)は未決着のまま保留されて
いる。動画→3D化を完全に止めるとは決めていない(併用の可能性も残る)。

## 目的

「3Dで、VTuberも3Dで、可愛く」(`VEO_VIDEO_TO_3D_PLAN.md`§17参照)という
元の目的に対し、リアルタイム3Dレンダリングや動的画像生成を使わず、**事前
生成した静的アセット(パーツ×角度×表情の組み合わせ)+実行時はLLMによる
テキスト推論だけで状態遷移を決める**、軽量なキャラクター駆動エンジンを
作る。ユーザー原案(以下、要旨を維持したまま整理して記録):

- **アセット生成パイプライン(オフライン)**: ベースイラストから、ローカル
  画像生成AIで表情・アクション・360度方向の差分アセットを網羅的にバッチ
  生成する。出力は透過背景の2D画像群(WebP等)。
- **推論・ルーティングエンジン(実行時)**: 配信コメント・音声テキスト・
  ゲーム内イベント等をLLM(Gemini API等)に渡し、次に表示すべき画像ID
  (JSON、例: `{"image_id":"smile_01"}`)を決める。
- **クライアント描画エンジン(実行時)**: 画像IDに基づき静的アセットを
  描画する。2Dモード(配信UIオーバーレイ)/3Dモード(3D空間内のビルボード、
  常にカメラに正対)の両対応。切り替え時は数フレームのクロスフェード。
- **非機能要件**: クライアント負荷最小化(2Dテクスチャ差し替えのみ)、
  実行時コスト最小化(推論はテキストLLMのみ、画像生成は実行時に一切
  行わない)、保守性(新アクション追加はオフライン生成+プロンプト微修正
  のみで完結)。

## 議論を通じた設計の深化

### 1. 全身ビルボード(N方向)の妥当性

角度を8方向(45度刻み)にすると回転のカクつきが目立つ(往年のドット絵
ゲームの妥協点)。32方向(11.25度刻み)+クロスフェードなら回転はかなり
滑らかに見える。ストレージコストも透過WebP数十〜百枚程度で問題にならない
(64方向でも同様)。ただし**仰角(見上げる/見下ろす)はこの方式でカバー
できない**(N方向は水平回転のみを指すため)。`controller.html`が一人称/
自撮り視点でカメラがだいたい目線の高さに留まるなら実害は小さいはずだが、
明確な限界として認識しておく。

### 2. 「スーパーLive2D」構想(パーツ分割+AI生成、本命案)

Live2Dは少数の絵をリアルタイム2D変形(ワープ)して立体回転を模擬するため、
極端な角度で破綻しやすい。これを「パーツごとにAI生成した実角度の絵を
差し替える」方式に置き換える構想:

- **組み合わせ爆発が抑えられる**: 表情は目・口だけ差し替えれば済み、体の
  ポーズ・角度と独立に組み合わせられる。髪の揺れも髪パーツだけの差し替えで
  完結し、全身を撮り直す必要がない。
- **ツインテール細り問題が構造的に消える**: 3D幾何を「推定」する工程が
  無くなるため、visual hullのシルエット交差限界そのものが発生しない。
  ツインテールは実際にその角度で生成された絵をそのまま切り出すだけなので、
  どの角度でも見た目が破綻しない。
- **新たに生まれる課題**: パーツごとの前後関係(Z順)が角度によって
  入れ替わる(例: 片方のツインテールは正面では体の前、背面付近では後ろ)。
  角度バケットごとにZ順メタデータを持たせる対応が要る。3D復元の難問より
  遥かに扱いやすいが、ゼロではない。

**進め方**: 全身ビルボード(既存のフレーム抽出+背景除去だけで完結)を
先に動かし、「可愛い・動く」を確認してから、パーツ分割(表情・髪の独立
差し替え)を第2段階として重ねるのが現実的(まだ決定ではない、選択肢の
一つ)。

### 3. 汎用化のための標準化(このキャラクター専用にしない)

将来的に任意のキャラクターへ対応させるには、以下3点の標準化が要る:

1. **パーツの標準タクソノミー**: 顔・目・口・前髪・後ろ髪・体・腕・脚を
   「コア」(全キャラ共通)、ツインテール・リボン・帽子等を「オプション」
   (キャラごとに可変)とする、コア+拡張の構成。**このリポジトリに既に
   同じ発想の実装がある**: `landmark_tool.html`のアクセサリー体系
   (`js/accessories.js`、`js/common.js`の`LM_GROUP_ORDER`)は「体は共通、
   アクセサリーはキャラごとに可変」を既に扱っている。ゼロ設計せず、ここを
   土台にできる。
2. **表情の標準タクソノミー**: 独自定義せず**VRMの表情プリセット**
   (happy/angry/sad/relaxed/surprised + まばたきL/R + リップシンク用
   viseme(aa/ih/ou/ee/oh))を採用する。`VEO_VIDEO_TO_3D_PLAN.md`§17の
   VRM出口(M7)ロードマップとも自然に繋がる。
3. **パーツごとの必要角度数**: 全パーツ一律32方向にする必要はなく、体・頭は
   多め、小さい装飾は少なめ、といった重要度に応じた角度数をマニフェスト
   (JSON)で持たせる。

### 4. 生成手段: Veo(動画) vs ローカル画像生成AI

Veo(動画生成)とローカル画像生成AIは**解く問題が違う**:

- **Veoが強いのは「1回のバッチで一貫性のある360度回転を撮る」こと**。裏を
  返すと、表情違い・ポーズ違いを都度別動画で撮ろうとすると、生成のたびに
  キャラクターが微妙にブレるリスクがある(別々の生成呼び出しをまたいだ
  一貫性は保証されない)。また、**1回の生成が数分単位・API課金・トライ
  アンドエラーに向かない**(ユーザー指摘、2026-07-19)。
- **大量の組み合わせ(パーツ×角度×表情)を作るにはローカル画像生成AIの
  方が向く**。キャラクターごとに軽量LoRAを1回学習(またはIP-Adapterで
  元絵1枚を参照条件として使う、学習なし)すれば、同一キャラクターである
  ことを保ったままControlNetで角度・ポーズだけを制御して量産できる。

**推奨スタック(未着手)**: ComfyUI + SD1.5系(アニメ塗り寄りのチェック
ポイント)+ ControlNet(角度/ポーズ制御)+ IP-Adapter(学習なしで開始、
反復向き。デザインが固まったらLoRAに切り替えて量産時の一貫性を上げる)。

**角度ガイドの作り方**: Veoの動画は不要。汎用の人型プロキシメッシュ
(Tポーズのマネキン)をThree.js(このリポジトリが既に使用)でN方向から
素朴にレンダリングするだけで、ControlNet用の角度ガイド(深度マップ/
OpenPoseスケルトン等)一式が作れる。`VEO_VIDEO_TO_3D_PLAN.md`の
turntable_calib.js/multiview_hull.jsで組んだ座標系(θ・スケール・原点の
投影の考え方)がそのままガイド生成の設計に流用できる(動画からの3D復元は
不要になったが、座標の枠組み自体は無駄になっていない)。

### 5. ハードウェア制約(重要、2026-07-19確認済み)

ユーザーはスマホしか持っておらず、生成はClaudeのサンドボックス上で行う
ことを希望。**このサンドボックス環境にはGPUが無いことを確認済み**
(`nvidia-smi`不在、CPU4コア、RAM15GB、ディスク空き30GB、2026-07-19実機
確認)。CPU限定でのStable Diffusion系生成は1枚あたり数分〜数十分かかり、
「パーツ×32方向×表情」規模(数百枚)を素直に生成すると数十〜百時間規模に
なり非現実的。加えてこの環境は**非活動状態が続くと回収される**(セッション
が消える)ため、長時間の無人バッチ生成には向かない。

**現実的な対応(未着手、次回以降の検討事項)**:
- LCM/Turbo系の蒸留モデル(4〜8ステップで生成できるSD派生)を使い、通常
  20〜50ステップ必要な生成を大幅に短縮する。
- 「数百枚」を一度に狙わず、まず環境構築+小規模テスト生成(1パーツ・数
  方向)を行い、①実際に動くか、②実機での1枚あたりの所要時間、③キャラ
  クター一貫性の質、を実測してから全量生成の進め方(Routineでの細切れ
  運用等)を決める。
- ユーザーの明示指示: 「まだしないで」(2026-07-19)。環境構築・生成は
  まだ着手していない。

## 未決着・次回への引き継ぎ

1. Stage C「hull=シルエット整合が上限」設計のゼロベース再考は保留中
   (`VEO_VIDEO_TO_3D_PLAN.md`側の話。本アセットエンジンとは独立に、
   動画→3D化を続ける場合に再開すべき論点として残る)。
2. 本計画は全てアイデア段階。次に着手するとすれば:
   a. パーツ/表情の標準マニフェスト(JSON)の設計
   b. 角度ガイド生成ツール(Three.jsプロキシメッシュ→N方向レンダリング、
      すぐ着手できる)
   c. ローカル画像生成AI環境構築+小規模テスト生成(実測値取得。ユーザー
      からの明示的な着手指示待ち)
3. 動画→3D化(Veoターンテーブル経由)とこのアセットエンジンを併用するか、
   完全に置き換えるかは未決定。
