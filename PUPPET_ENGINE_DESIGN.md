# ピクセルパペットエンジン設計書(2026-07-19、v2→v3改訂)

> **v3改訂(2026-07-20、ユーザーレビュー反映)**: v2実装(旧キャラ
> silver_ponytailのMVP、Live2D比30点評価)の知見から2つの大転換を実施。
>
> 1. **レンダラをグリッドメッシュワープに全面書き換え**(`puppet/viewer.html`)。
>    レイヤー剛体移動を廃止し、全体を一枚合成→16x18三角形メッシュの連続変形場で
>    描画。頭・髪・体のつなぎ目が原理的に出ない。全パラメータは2次ばね駆動。
> 2. **キャラをゼロベースで再生成**(ユーザー指示「ゼロベースで最高目指す」
>    「面影は残す」)。旧原画はマイクが口を横切る等リグ不適だったため、面影を
>    維持したimg2img再構成で新マスターを量産→ユーザー選定
>    `bust_st60_s7`→ゴースト除去+顔hires-fix済みが
>    **`characters/zero/master_bust.png`(現行マスター)**。
>    全身版`characters/zero/adopted/master_st60_s7.png`は3Dゲーム用。
>    自家生成マスターなので表情派生の同一性保持が旧キャラより格段に安定する。
>
> **ロードマップ(Live2D比の自己評価30点→)**:
> 30→55点: パーツ別デフォーマ/目の内部分解(虹彩スライド・まぶた変形)/
> 髪の多段振り子/口形状モーフ(全てエンジン側、生成不要)。
> 55→75点: 首振り±15°疑似3D(デフォーマ調整をQCループで収束)。
> 75点〜: ビューリング生成が必要(上位計画v2の疎格子)。
>
> 以下のv2本文のうち、§Aのレイヤー構成・生産ラインの考え方は有効。
> 実現手段はメッシュワープ+新マスターに読み替える。

> **v2改訂(MVP実装知見の反映)**: 目・口の小パッチ分離とプログラム描画・
> 下絵ガイド方式は実装検証の結果**品質不足で廃止**(ユーザー指摘)。
> 「画像は大量生成可能」を前提に、§Aの
> **キーフレームバンク+レイヤー合成**へ全面転換した。§1の機能一覧は
> 有効だが、実現手段は§Aが正となる。キャラクターは
> `characters/silver_ponytail/full.jpg`(上半身)に変更。

## A. 実現アーキテクチャ(v2、これが正)

**変化の速さで2階建てに分ける:**

1. **下の階=連続的な動き**: 体・髪・ヘッドセットは原画ピクセルのまま、
   実行時変形だけ行う(呼吸・頭ボブ・ポニーテールVerletばね・クロスフェード・
   目領域の数px局所ワープによる連続視線)。生成AI不関与。実証済み。
2. **上の階=離散的な状態**: 表情は**顔の内側領域(固定フェザーマスク)を
   まるごと1枚**で差し替える。目・口を独立パッチにしない。
   状態ごとに候補を大量生成し選別して採用(=品質はサンプル数で出す)。

**組み合わせ爆発の解法**: viseme(口パク)差分は採用済み表情フレームを
親としてimg2img派生させる。目・頬が親と一致するので口領域の整合が保たれる。
必要な表情(主にneutral/joy)の分だけ作る。

**生産ライン(全アセット共通の工場)**:
バッチ生成(無人) → 自動フィルタ(フェザー境界帯のSSIM・派生の親一致度で
機械採点) → コンタクトシート目視選別 → マニフェスト登録 → ビューア確認。
表情・viseme・視線変化・ポーズ・衣装・角度リング・ダンスキーフレーム、
すべてこの同じループに流す。

**Live2D比の到達点**: まばたき・口パク・物理・表情はLive2D級
(口形状ブレンドはLive2Dも離散)。±10〜15°の首振りはレイヤー視差+微ワープで
疑似3D化。±30°級の連続回転のみビューリング整備(疎格子)で漸近させる。
角度ごとの絵の破綻がない分、大角度ではLive2D超えが狙い。

**明示的に廃止**: 目・口の独立小パッチ/プログラム描画の口・目/
下絵ガイド方式/ワープによる表情生成(ワープは補間糊・視線微シフトのみ)。

> 画像生成パイプラインの実証完了([`ON_DEVICE_GENERATION_PLAN.md`](./ON_DEVICE_GENERATION_PLAN.md)§8)
> を受けた、次段階=エンジン本体の設計目標。実装はユーザーの明示指示があるまで
> 行わない。上位計画は[`REAL_ART_LIVE2D_PLAN.md`](./REAL_ART_LIVE2D_PLAN.md)(v2)。
> 本書はそのv2の§2段5「書き出し」と、旧
> [`VTUBER_ASSET_ENGINE_PLAN.md`](./VTUBER_ASSET_ENGINE_PLAN.md)のLLM
> ルーティングエンジン構想を統合・具体化したもの。

## 0. 要件(ユーザー確認済み、2026-07-19)

1. **VTuberをやりたい**
2. **可愛くしたい**
3. **3Dゲームにも使いたい** — ただし**3Dモデル(メッシュ)は作らない**。
   キャラは常にカメラ正対のビルボードスプライト。3Dはあくまで背景・空間側。

設計原則は上位計画から継承: 可愛さは常に2D由来/全体再生成しない/
実絵キーフレーム主役・ワープは補間糊/疎格子。

## 1. 機能一覧(何を目指すか)

### 1.1 表情・顔

- **表情プリセット**: VRM標準(happy/angry/sad/relaxed/surprised)を採用。
  独自定義しない。
- **まばたき**: L/R独立。
- **リップシンク**: viseme 5形状(aa/ih/ou/ee/oh)。会話機能のTTS音声と同期。
- **視線**: 瞳(瞳孔+ハイライト)だけを目パーツ内でスライドさせ、
  カメラ目線・横目・上目遣いを表現。実装コストは瞳レイヤーの平行移動のみ。

表情はすべて**顔パーツ(目・口・眉)だけの差し替え**で実現し、体・衣装・
角度と独立。

### 1.2 体・アクション

- **待機モーション**: 呼吸ゆらぎ・重心の微小スイング(プロシージャル、生成不要)。
- **基本アクション(単発)**: 挨拶(手を振る)・頷き・驚き(のけぞり)等。
  少数キーフレーム+ワープ補間。
- **ダンス**: 複数キーフレームを時間軸で繋ぐ「アクションクリップ」形式。
  **第2段階**とする — まず単発アクションでキーフレーム間ワープ補間の品質を
  実証してから着手(補間が破綻する振り付けはキーフレーム増で対処するため、
  コストが読めるようになってから)。
- タッチ反応: **不採用**(ユーザー判断、2026-07-19)。

### 1.3 衣装替え

- 衣装は**独立パーツレイヤー**。`ghostitd`の`accessories.js`
  「コア(全キャラ共通)+オプション(キャラごと可変)」構成にそのまま乗せる。
- 組み合わせ爆発は起きない: 表情は顔パーツのみ・衣装は体パーツのみなので
  掛け算にならない。衣装ごとに追加コストが掛かる軸は**角度のみ**
  (角度ごとにシルエットが変わるため)。
- ポーズとの依存は要検証(未決着事項§4参照)。

### 1.4 角度(疎格子、上位計画v2から継承)

| 角度帯 | 分解レベル | アセット |
|---|---|---|
| 正面±45°(3ビュー) | **フル分解**: 全パーツ+表情+口パク+視線 | 生成+レイヤー分解 |
| ±45°〜±135°の中間 | 簡易分解: 一枚絵+髪揺れレイヤー | 新規ビュー合成で生成 |
| 背面(180°) | 簡易分解: 既存back.png+髪揺れレイヤーのみ | **生成不要**(検収済み原画) |

背面に表情差分を持たせない理由: 顔が写らないため。3Dゲームで回り込んだ
時の滑らかさは、簡易分解の中間ビュー+クロスフェードで確保する。

### 1.5 物理

- `poc/vector_puppet_poc.html`で実証済みの**Verletばね**を流用。
  髪(ツインテール)・スカート・スカーフの揺れ。全角度帯に適用
  (簡易分解ビューでも髪揺れレイヤーだけは分離するのはこのため)。

### 1.6 会話機能(LLMルーティング+音声AI)

旧`VTUBER_ASSET_ENGINE_PLAN.md`の「推論・ルーティングエンジン」構想を復活・統合:

```
ユーザー入力(チャット欄 or 配信コメント)
  → LLM(返答文 + 表情/アクションIDをJSONで返す。実行時画像生成は一切なし)
  → TTS(音声AI)が返答文を読み上げ
  → 音声の音素/音量からvisemeを駆動して口パク
  → 同時に選ばれた表情・アクションに遷移
```

- **TTS候補**(未選定、要調査・実測):
  - VOICEVOX等の無料ローカル系(品質・キャラ声。サーバプロセスが必要)
  - ブラウザ内蔵Web Speech API(依存ゼロで最軽量。声質は端末依存)
  - クラウドTTS(品質最高だが実行時課金・オフライン不可)
- 実行時コスト最小化の原則(テキストLLM+静的アセットのみ)は維持。
  TTSだけが追加の実行時処理となる。

### 1.7 入力系統(3系統に整理)

1. **MediaPipe自動トラッキング**: カメラの顔向き・まばたき・口を反映
   (スマホ内ローカル)
2. **手動ボタン**: ビューアのボタンで表情・アクション・衣装・角度を単発トリガー
3. **会話駆動**: LLM+TTSが表情・口パク・アクションを自動選択

競合時の優先順位: 会話駆動の発話中はvisemeが口を占有 > 手動ボタン >
自動トラッキング(ベースレイヤー)。

## 2. ビューア(新規コンポーネント)

表情・アクション・衣装・角度・視線を**ボタンで選んで確認できる**ブラウザツール。
単一HTML(既存PoCと同じ流儀)。役割は2つ:

1. **アセットQC**: 生成した差分をカメラ無しで一覧・切替チェック
   (Claude自身の視覚QCループにも使う)
2. **配信時の手動トリガー**: Live2D系ソフトのホットキー相当。
   チャット欄を付ければ§1.6の会話機能のフロントエンドを兼ねる

ビューアとエンジン本体は同じ描画コア+同じマニフェストを読む
(ビューア=エンジンのデバッグUI付き版、という位置づけ)。

## 3. データ構造(JSONマニフェストが正)

上位計画v2の§2段3で宣言済みの方針を継承。1キャラ=1マニフェストに:

- パーツ定義: 切り出し領域・アンカー・スパイン(コア+オプション構成)
- **角度バケットごとのZ順**(ツインテールが正面では前・背面付近では後ろ、等)
- 表情差分の登録(目・口・眉の差し替え領域)
- 衣装レイヤーの登録
- アクションクリップ(キーフレーム列+タイミング)
- 物理パラメータ(ばね定数・減衰)

**3Dゲーム側の書き出しもこのマニフェストを正とする**: スプライト
(ヘッドレスChromiumでN方向×表情を一括レンダリング)+マニフェストの組で
納品し、ゲームエンジン(three.js/Godot)側はそれを読むだけにする。

## 4. 未決着事項(実装前に順次解消)

1. **ダンスのキーフレーム予算**: 1クリップ=キーフレーム何枚までか。
   単発アクションのワープ補間品質を見てから決める(§1.2)。
2. **アクション×衣装の依存**: 腕を上げると袖のシルエットが変わる衣装
   (現行キャラのオフショルダー等)で、衣装1セットをポーズ間で使い
   回せるかは仮説段階。最初の衣装替え実装時に検証。
3. **TTS選定**: §1.6の3候補の品質・レイテンシ・導入コストを比較していない。
4. **LLM接続先**: 会話のLLMをどこで動かすか(Gemini API等のクラウドか、
   配信PC/スマホ内か)。実行時課金と応答速度のトレードオフ。

## 5. 着手順(提案、実装はユーザー指示待ち)

1. **ビューアの骨格+正面フル分解**(上位計画v2のMVP「正面のみVTuber
   パペット」と同一。既存front.pngから生成AI無しで作れる):
   レイヤー分解→まばたき・口パク・視線・表情ボタン→ビューアで確認
2. 単発アクション(挨拶・頷き)でワープ補間品質を実証 → ダンス予算決め
3. 会話機能: チャット欄+LLMルーティング+TTS(まずWeb Speech APIで
   最小実装→品質不満ならVOICEVOX)
4. 衣装替え1着追加(アクション依存の検証込み)
5. 中間ビュー・背面の組み込み(画像生成パイプラインは実証済み)
6. スプライト書き出し→3Dゲーム側ビルボード実証
7. ダンス(アクションクリップ)

## 6. 関連文書

- [`REAL_ART_LIVE2D_PLAN.md`](./REAL_ART_LIVE2D_PLAN.md) — 上位計画(v2)
- [`ON_DEVICE_GENERATION_PLAN.md`](./ON_DEVICE_GENERATION_PLAN.md) —
  画像生成パイプライン(§8で実証済み: MeinaMix V10/V11+OpenVINO、
  サンドボックスで1枚8〜100秒)
- [`VTUBER_ASSET_ENGINE_PLAN.md`](./VTUBER_ASSET_ENGINE_PLAN.md) —
  LLMルーティングエンジン構想の初出(本書§1.6に統合)
- `poc/vector_puppet_poc.html` — Z順ソート・Verletばね・視覚QCループの実証元
